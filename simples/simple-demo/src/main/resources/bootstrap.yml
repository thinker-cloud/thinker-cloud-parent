server:
  port: 8088
  undertow:
    buffer-size: 1024

spring:
  application:
    name: @artifactId@
  profiles:
    active: ${PROFILES_ACTIVE:thinker}

springdoc:
  api-docs:
    enabled: true
  group-configs:
    - group: 'default'
      packages-to-scan: com.example.demo.controller
